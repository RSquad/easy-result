import { ContestStatePipe } from './contest-state.pipe';

describe('ContestStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ContestStatePipe();
    expect(pipe).toBeTruthy();
  });
});
